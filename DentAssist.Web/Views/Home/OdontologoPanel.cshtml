@{
    ViewBag.Title = "Panel del Odontólogo";
}

<div class="container mt-5">
    <h2 class="fw-bold text-primary text-center mb-3">
        <i class="bi bi-person-badge me-2"></i>
        Panel del Odontólogo
    </h2>
    <p class="text-center mb-4">
        Desde aquí puedes gestionar tu <strong>agenda</strong> y revisar los <strong>planes de tratamiento</strong> asignados a tus pacientes.<br />
        Accede a cada sección desde los accesos directos.
    </p>

    <div class="row justify-content-center mb-5">
        <div class="col-md-5 mb-3">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body text-center">
                    <i class="bi bi-calendar-check display-4 text-primary"></i>
                    <h5 class="fw-bold mt-2 mb-2">Mi Agenda</h5>
                    <p class="text-muted">Revisa y gestiona los turnos agendados contigo.</p>
                    <a href="@Url.Action("MiAgenda", "Turno")" class="btn btn-primary">Ir a mi Agenda</a>
                </div>
            </div>
        </div>
        <div class="col-md-5 mb-3">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body text-center">
                    <i class="bi bi-clipboard2-pulse display-4 text-success"></i>
                    <h5 class="fw-bold mt-2 mb-2">Planes de Tratamiento</h5>
                    <p class="text-muted">Visualiza y administra los planes activos de tus pacientes.</p>
                    <a href="@Url.Action("MisPlanes", "PlanTratamiento")" class="btn btn-success">Ver Planes</a>
                </div>
            </div>
        </div>
    </div>
    <footer class="text-center mt-4 small text-muted">
        © @DateTime.Now.Year — DentAssist
    </footer>
</div>
