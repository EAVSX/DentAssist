{
    // Archivo de configuración para definir la cadena de conexión a la base de datos.
    // Se conecta a una base MySQL llamada "DentAssistDb" en localhost con usuario "root" y clave "admin".
    "ConnectionStrings": {
        "DefaultConnection": "server=localhost;port=3306;database=DentAssistDb;user=root;password=admin;"
    }
}
